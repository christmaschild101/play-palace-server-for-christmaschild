# Ship, Captain & Crew dice game messages

# Game info
game-name-shipcaptaincrew = Ship, Captain & Crew

# Actions
shipcaptaincrew-roll = Roll the dice
shipcaptaincrew-bank = Bank { $score } and end turn

# Rolling
shipcaptaincrew-you-rolled = You rolled: { $dice }.
shipcaptaincrew-rolled = { $player } rolled: { $dice }.
shipcaptaincrew-ship-found = { $player } found the ship (6)!
shipcaptaincrew-captain-found = { $player } found the captain (5)!
shipcaptaincrew-crew-found = { $player } found the crew (4)!
shipcaptaincrew-no-rolls-left = { $player } is out of rolls.

# Banking
shipcaptaincrew-you-banked = You bank { $score } points (total { $total }).
shipcaptaincrew-banked = { $player } banks { $score } points (total { $total }).

# Game end
shipcaptaincrew-winner = { $player } wins with { $score } points!
shipcaptaincrew-tie = { $players } tie with { $score } points!
shipcaptaincrew-score-line = { $player }: { $score }

# Options
shipcaptaincrew-set-target = Target score: { $score }
shipcaptaincrew-desc-target = First player to reach this total wins (when rounds are off)
shipcaptaincrew-enter-target = Enter target score:
shipcaptaincrew-option-changed-target = Target score set to { $score }.
shipcaptaincrew-set-rounds = Rounds: { $rounds }
shipcaptaincrew-desc-rounds = Number of rounds to play (0 = first to target score)
shipcaptaincrew-enter-rounds = Enter number of rounds (0 = first to target):
shipcaptaincrew-option-changed-rounds = Rounds set to { $rounds }.

# Disabled reasons
shipcaptaincrew-no-rolls = No rolls left.