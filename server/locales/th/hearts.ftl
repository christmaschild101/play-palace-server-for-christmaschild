# Hearts card game messages

# Game info
game-name-hearts = Hearts

# Actions
hearts-pass = Pass a card ({ $count } left)
hearts-pass-done = Done passing
hearts-play = Play a card
hearts-status = Check the trick
hearts-pick-pass-card = Pick a card to pass:
hearts-pick-card = Pick a card to play:
hearts-status-label = Trick: { $trick } — hearts broken: { $broken }
hearts-status-info = The trick so far: { $trick }. Hearts broken: { $broken }.

# Pass modes
hearts-pass-left = pass left
hearts-pass-right = pass right
hearts-pass-across = pass across
hearts-pass-none = no passing

# Hand flow
hearts-hand-start = Hand { $round }. Everyone gets thirteen cards.
hearts-pass-now = Time to { $mode }.
hearts-pass-complete = Cards are passed. Time to play!
hearts-no-pass = No passing this game.
hearts-picked-three = { $player } is ready.

# Trick play
hearts-trick-lead = { $player } leads the trick.
hearts-played = { $player } plays { $card }.
hearts-trick-won = { $player } wins the trick!
hearts-broken = Hearts are now broken!

# Scoring
hearts-hand-scores = Scores after this hand: { $scores }
hearts-moon = { $player } shot the moon! Minus twenty-six points!
hearts-winner = { $player } wins with the lowest score of { $score }!
hearts-tie = { $players } tie with { $score }!
hearts-score-line = { $player }: { $score }

# Disabled reasons
hearts-not-passing = Not the passing phase.
hearts-not-playing = Not the play phase.
hearts-already-picked-three = You already picked three cards.
hearts-pick-three-first = Pick three cards first.
hearts-must-follow = You must follow suit if you can.

# Options
hearts-set-target = Target score: { $score }
hearts-desc-target = Game ends when any player reaches this total; lowest wins
hearts-enter-target = Enter target score:
hearts-option-changed-target = Target set to { $score }.
hearts-set-pass = Passing: { $mode }
hearts-desc-pass = Which way to pass cards each hand
hearts-select-pass = Select passing direction:
hearts-option-changed-pass = Passing set to { $mode }.